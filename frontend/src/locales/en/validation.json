{
  "email": {
    "required": "Email is required",
    "invalid": "Invalid email address"
  },
  "password": {
    "required": "Password is required",
    "minLength": "Password must be at least {{count}} characters",
    "confirmRequired": "Password confirmation is required",
    "mismatch": "The passwords don't match"
  },
  "name": {
    "required": "Name is required",
    "fullNameRequired": "Full Name is required",
    "invalid": "Only lowercase letters, numbers, and hyphens allowed, cannot start or end with hyphen"
  },
  "reason": {
    "required": "Reason is required",
    "minLength": "Reason must be at least {{count}} characters"
  },
  "template": {
    "required": "Template is required"
  },
  "os": {
    "required": "Operating system is required"
  },
  "username": {
    "required": "Username is required"
  },
  "diskSize": {
    "required": "Disk size is required"
  },
  "requirement": {
    "lxc": "LXC container requires OS template and disk size",
    "vm": "VM requires OS, username, and disk size"
  }
}
